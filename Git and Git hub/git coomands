1. git init - used to create an empty Git repository.
2. git add -  to add files to the staging area.
3. git commit - sure that the changes are saved to the local repository
4. git commit -m "message" - allows you to describe everyone and help them understand what has happened.
5. git status -  git status command tells the current state of the repository.
6. git config - git config command is used initially to configure the user.name and user.email.
7. git branch - git branch command is used to determine what branch the local repository is on.

  # Create a new branch
  git branch <branch_name>
  git branch -M main

  # List all remote or local branches
  git branch -a

   # Delete a branch
  git branch -d <branch_name>

8. git checkout - git checkout command is used to switch branches,
9. git merge - git merge command is used to integrate the branches together
10. git remote - git remote command is used to create, view, and delete connections to other repositories. 

    git remote add origin <address>

11. git clone - git clone command is used to create a local working copy of an existing remote repository.
                 Everythimg is pulled

    git clone <remote_URL>

12. git pull -  git pull command is used to fetch and merge changes from the remote repository to the local repository
                only the thing changes is pulled

    git pull <branch_name> <remote URL>

13. git push - To push the local changes to the Repository

    git push -u origin main

14. git CHERRY-PICKING
       Cherry picking is the act of picking a commit from a branch and applying it to another.
Git commits to be picked by reference and appended to the current working HEAD. 
git cherry-pick can be useful for undoing changes.

15. git Reset
    Removing the commit completely or permenently.
    git Reset -hard commit id(just be the one which you want to remove)

16. git revert
    Commit is still there just temporary delete.Replicate the commit before deleting.
    git rever -HEAD

17. git stash
    If you have codebor work which is temporary or incomplete and also this is not committed then run git stash.uncompleted work store in stash folder.

    you can retrive this by running git statsh pop

16. git stash drop - removes the most recent stash entry.

19. To print all commits in the specified date range
      git log --since=<date-from> --untill=<date-to>

19. git add -A - All new and updated files are staged.

20. git remote -v - A list of remote repositories and their urls

21. git remote -i HEAD 10  - To list the last 10 commits and their modifications.

22. git tag -  To pause the code and nobody can chane the code(whenever you go to long vacation, he work have to pause for sometime)
                git tag --a  <de-freeze>(tagname)  <commit id> -m
  
  


